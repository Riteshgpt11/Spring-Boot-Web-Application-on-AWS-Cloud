version: 0.0
os: linux

files:
  - source: ./appspec.yml
    destination: /home/ubuntu
  - source: ./startup.sh
    destination: /home/ubuntu
  - source: ./build/libs/ROOT.war
    destination: /opt/tomcat/webapps

hooks:
  AfterInstall:
   - location: ./startup.sh
     timeout: 180
     runas: ubuntu
